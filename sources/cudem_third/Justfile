# NOAA CUDEM 1/3 arc-sec (offshore band + Alaska). Run from pipelines/:  just ../sources/cudem_third/
# Streaming: register every manifest tile as a /vsicurl reference (header reads only,
# no download). ~10 m, NAVD88 / NAD83; already COGs → no normalize/datum/tarball.
# Priority derives from (maxzoom, id): cudem (z13) wins over cudem_third (z12) over gebco.
[no-cd]
default:
    uv run python source_register_remote_urllist.py cudem_third
